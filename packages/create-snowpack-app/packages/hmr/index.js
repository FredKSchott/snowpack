/**
 * This is the production @snowpack/hmr code.
 *
 * `snowpack dev` - Replaces this with the hydrated hmr logic.
 * `snowpack build` - Uses this empty stub as a no-op.
 */
export function apply() {}
