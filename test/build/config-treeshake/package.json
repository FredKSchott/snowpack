{
  "private": true,
  "version": "1.0.1",
  "name": "@snowpack/test-config-treeshake",
  "description": "This test ensures that --treeshake can handle complex imports",
  "scripts": {
    "testbuild": "snowpack build --treeshake --no-minify"
  },
  "snowpack": {
    "mount": {
      "./src": "/_dist_"
    }
  },
  "dependencies": {
    "async": "^3.2.0"
  },
  "devDependencies": {
    "snowpack": "^2.7.0"
  }
}
