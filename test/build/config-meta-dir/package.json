{
  "private": true,
  "version": "1.0.1",
  "name": "@snowpack/test-config-meta-dir",
  "description": "Test that makes sure that the metaDir can be configured and still build",
  "scripts": {
    "prepare": "snowpack",
    "testbuild": "snowpack build"
  },
  "dependencies": {
    "shallow-equal": "^1.2.1"
  },
  "devDependencies": {
    "snowpack": "^3.0.0"
  }
}
