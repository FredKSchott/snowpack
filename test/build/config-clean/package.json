{
  "description": "Test for buildOptions.clean",
  "scripts": {
    "TEST": "node ../../../pkg/dist-node/index.bin.js build"
  },
  "snowpack": {
    "buildOptions": {
      "clean": false,
      "optimize": false
    },
    "scripts": {
      "mount:src": "mount src --to /_dist_"
    }
  }
}
