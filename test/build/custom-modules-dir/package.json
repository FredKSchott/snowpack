{
  "private": true,
  "version": "1.0.1",
  "name": "@snowpack/test-custom-modules-dir",
  "description": "This test ensures that Snowpack can build when /my_meta is the web modules dir",
  "scripts": {
    "testbuild": "snowpack build"
  },
  "dependencies": {
    "array-flatten": "^3.0.0"
  },
  "devDependencies": {
    "snowpack": "^3.0.0"
  }
}
