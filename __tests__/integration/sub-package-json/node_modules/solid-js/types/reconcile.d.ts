import { Wrapped, NotWrappable } from "./state";
declare type ReconcileOptions = {
    key?: string | null;
    merge?: boolean;
};
export declare function reconcile<T>(value: T | Wrapped<T>, options?: ReconcileOptions): (state: T extends NotWrappable ? T : Wrapped<T>) => void;
export {};
